.DEFAULT_GOAL := compile

N = 100
THREADS_PER_BLOCK = 10

HPC_FLAGS = -O3
COMPILER = nvcc
PARAMETERS = -D N=${N} -D THREADS_PER_BLOCK=${THREADS_PER_BLOCK}
SRC = array_reverse.cu

compile:
	$(COMPILER) $(HPC_FLAGS) $(PARAMETERS) $(SRC)
output:
	$(COMPILER) $(HPC_FLAGS) $(PARAMETERS) -D OUTPUT $(SRC)